<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.NeoRomax.HostelTonight.Rsv.Dao.RsvDao">
	<select id="rsvList" resultType="com.NeoRomax.HostelTonight.Rsv.Dto.RsvAvailableDto">
		<![CDATA[select * from reservation where RSVHOSTELNUM = #{param1} and RSVDATE >=#{param2} and RSVDATE <= #{param3}]]>
	</select>
	<insert id="RsvConfirm">
			insert into reservation_confirm (hostelNum, roomNum, userId, totalRate) values(#{param1},#{param2},#{param3},#{param4});
	</insert>
</mapper>