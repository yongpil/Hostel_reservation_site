		 	 (function myFunction(){
		 	    var result = new Array();
		 	   <c:forEach items="${RsvAbleDto}" var="info">
		 	   		var json = new Object();
		 	   		json.rsvDate="${info.rsvDate}";
		 	   		json.rsvRate="${info.rsvRate}";
		 	   		result.push(json);
		 	   	</c:forEach>
		 	     }());
		 	     
			var app = angular.module('app',[]);
			
			app.controller('ListController', function($scope) {
			    $scope.price = 0;
				$scope.sum = function(){
					if($scope.mySwitch==true)
						{
							$scope.price = $scope.price + 100;
							return $scope.price;
						}
					else if($scope.mySwitch==false)
						{
							$scope.price = $scope.price - 100;
			    			return $scope.price;
						}
			    }
			});
			
			
			
						if(x.checked==true)
				{
					rateArry[roomsNum_] = rateArry[roomsNum_] + '<%=rsvRoomList.get(roomsNum_).gettRsvAvailableDtos().get(index_).getRsvRate%>';	
				}
			else
				{
						rateArry[roomsNum_] = rateArry[roomsNum_] - '<%=rsvRoomList.get(roomsNum_).gettRsvAvailableDtos().get(index_).getRsvRate%>';
				}
				
2015/12/03
1.로그인 성공하면 session에 ID값 넣어주기
2.예약 버튼 누르면 session에 ID값이 저장되어 잇는지 확인하고 저장이 않되있으면 loginForm페이지로 저장이 되있으면 예약 페이지로 이동하기. 